{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Deploy",
            "request": "launch",
            "command": "npm run deploy",
            "cwd": "${workspaceFolder}"
        }
    ]
}